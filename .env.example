# Example only — run ./bootstrap.sh to generate a real .env with strong secrets.
AUTHENTIK_TAG=2024.10
PG_USER=authentik
PG_DB=authentik
PG_PASS=changeme
AUTHENTIK_SECRET_KEY=changeme-please-use-bootstrap.sh
COMPOSE_PORT_HTTP=9000
COMPOSE_PORT_HTTPS=9443
AUTHENTIK_LOG_LEVEL=info
